To run the firmware update program, navigate to this directory in a terminal and run the following command:

sudo ./FW_Update.sh

You will be prompted to continue during the process.

If you are running a desktop environment, you can easily open a terminal in this directory by right-clicking on an empty space in the open folder, then you can select Open in terminal.